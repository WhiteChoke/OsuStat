<UserControl x:Class="OsuStat.UI.MVVM.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OsuStat.UI.MVVM.View"
             xmlns:components="clr-namespace:OsuStat.UI.Components"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="572"
             Background="{StaticResource BackgroundBlack}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="2"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Margin="18 30 18 18">
                <Ellipse Height="60" Width="60" HorizontalAlignment="Left" >
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding User.AvatarPath }" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>
                <StackPanel VerticalAlignment="Bottom" Margin="15 0">
                    <TextBlock Text="{Binding User.Nickname}" VerticalAlignment="Center" FontSize="26"/>
                    <TextBlock Text="{Binding User.GlobalRanking}" FontSize="16"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" 
                        Orientation="Horizontal"  Margin="22">
                <TextBlock Text="last session:" FontSize="26"/>
                <TextBlock Text="{Binding User.LastSession}" FontSize="17" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel  Grid.Row="1" Margin="24 0">
                <TextBlock Text="Today" FontSize="40"/>
                <TextBlock FontSize="20" Text="{Binding PlayerStat.PlayTimeMin, StringFormat='• Play time {0}'}" Margin="6 0 0 10 "/>
                <TextBlock FontSize="20" Text="{Binding PlayerStat.MapPlayed, StringFormat='• Maps played {0}'}" Margin="6 0 0 10 "/>
                <TextBlock FontSize="20" Text="{Binding PlayerStat.PpGained, StringFormat='• PP gained {0}'}" Margin="6 0 0 10 "/>
                <TextBlock FontSize="20" Text="{Binding PlayerStat.AvgBpm, StringFormat='• AVG Bpm {0}'}" Margin="6 0 0 10 "/>
                <TextBlock FontSize="20" Text="{Binding PlayerStat.AvgStarRate, StringFormat='• AVG StarRate {0}'}" Margin="6 0 0 10 "/>
                <TextBlock FontSize="20" Text="{Binding PlayerStat.AvgAccuracy, StringFormat='• AVG Accuracy {0}'}" Margin="6 0 0 10 "/>
            </StackPanel>
            <StackPanel Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right" Margin="20 0">
                <TextBlock Text="Best score" FontSize="23"/>
                <Border Width="210" Height="210" 
                        CornerRadius="10" HorizontalAlignment="Left">
                    <Border.Background>
                        <ImageBrush ImageSource="{Binding BestScore.BgPath}" Stretch="UniformToFill" />
                    </Border.Background>
                    <Grid VerticalAlignment="Bottom" Margin="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Border CornerRadius="4" Background="#86000000" 
                                Margin="0 0 5 0" Padding="5 0">
                            <TextBlock Text="{Binding BestScore.MapName }" FontSize="20"/>
                        </Border>
                        <Border CornerRadius="4" Background="#86000000" 
                                Grid.Column="1" Padding="5 0" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding BestScore.Pp}" FontSize="20"/>
                        </Border>
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
        <Rectangle Grid.Row="1" Fill="White"/>
        <Border Grid.Row="2"
                Background="{StaticResource RaspberryRed}"
                CornerRadius="17"
                Margin="20">
            <ScrollViewer Margin="10 25">
                <ItemsControl ItemsSource="{Binding BeatMaps}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <components:Beatmap/>                        
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    
                </ItemsControl>
            </ScrollViewer>
            
        </Border>
    </Grid>
</UserControl>
